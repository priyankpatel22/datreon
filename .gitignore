# OS / editor junk
.DS_Store
Thumbs.db
*.log

# Local Claude Code config (not for the public repo)
# ...except the blog-routine ops files, which the scheduled cloud routine reads.
.claude/*
!.claude/blog-queue.json
!.claude/blog-routine.md

# Node (if tooling is added later)
node_modules/

# Local research material — do NOT publish to the live site
govcon-transcripts/
